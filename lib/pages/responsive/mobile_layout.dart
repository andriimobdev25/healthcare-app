
import 'package:flutter/material.dart';

class MobileSreenLayout extends StatelessWidget {
  const MobileSreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mobile screen"),
      ),
    );
  }
}